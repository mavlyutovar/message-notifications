<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Kafka Brokers
    |--------------------------------------------------------------------------
    | Список брокеров Kafka
    */

    'brokers' => env('KAFKA_BROKERS', 'kafka:9092'),

    /*
    |--------------------------------------------------------------------------
    | Consumer Group ID
    |--------------------------------------------------------------------------
    | Все consumer'ы внутри группы делят партиции
    */

    'group_id' => env('KAFKA_GROUP_ID', 'notification-service'),

    /*
    |--------------------------------------------------------------------------
    | Offset Reset Policy
    |--------------------------------------------------------------------------
    | earliest | latest
    */

    'offset_reset' => env('KAFKA_OFFSET_RESET', 'latest'),

    /*
    |--------------------------------------------------------------------------
    | Auto Commit
    |--------------------------------------------------------------------------
    | Автоматическое подтверждение offset'ов
    */

    'auto_commit' => env('KAFKA_AUTO_COMMIT', true),

    /*
    |--------------------------------------------------------------------------
    | Consumer Timeout (ms)
    |--------------------------------------------------------------------------
    */

    'timeout_ms' => env('KAFKA_CONSUMER_TIMEOUT', 2000),

    /*
    |--------------------------------------------------------------------------
    | Retry / Error handling
    |--------------------------------------------------------------------------
    */

    'retry_sleep_ms' => env('KAFKA_RETRY_SLEEP_MS', 100),

];
